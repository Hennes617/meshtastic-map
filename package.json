{
  "name": "meshtastic-map",
  "version": "1.0.0",
  "description": "Self-hosted MQTT-powered map for Meshtastic nodes and telemetry.",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "start:mqtt": "node src/mqtt.js",
    "admin": "node src/admin.js",
    "test": "jest",
    "check": "node --check src/index.js && node --check src/mqtt.js && node --check src/admin.js && jest --runInBand"
  },
  "author": "",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@prisma/client": "^5.22.0",
    "command-line-args": "^5.2.1",
    "command-line-usage": "^7.0.4",
    "compression": "^1.8.1",
    "express": "^5.2.1",
    "mqtt": "^5.15.2",
    "protobufjs": "^7.6.5"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "prisma": "^5.22.0"
  }
}
